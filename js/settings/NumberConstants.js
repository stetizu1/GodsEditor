const
    MAX_WIDTH = 3000,
    MAX_HEIGHT = 3000,
    //same as smallest cutout
    MIN_WIDTH = 320,
    MIN_HEIGHT = 480,
    THUMB_HEIGHT = 66,
    MEDIUM_WIDTH = 640;

const SMALL_CIRCLE_R = 10,
    BIG_CIRCLE_R = 20;

const SCREEN_WIDTH_LIMIT = 980,
    WIDTH_COEFFICIENT_BIG_SCREEN = 0.5,
    WIDTH_COEFFICIENT_SMALL_SCREEN = 0.9;

const GALLERY_CUTOUT_DEFAULT_HEIGHT = 160,
    GALLERY_CUTOUT_DEFAULT_WIDTH = 110;

class NumberConstants {
    static get MAX_WIDTH() {
        return MAX_WIDTH;
    }

    static get MAX_HEIGHT() {
        return MAX_HEIGHT;
    }

    static get MIN_WIDTH() {
        return MIN_WIDTH;
    }

    static get MIN_HEIGHT() {
        return MIN_HEIGHT;
    }

    static get THUMB_HEIGHT() {
        return THUMB_HEIGHT;
    }

    static get MEDIUM_WIDTH() {
        return MEDIUM_WIDTH;
    }

    static get SMALL_CIRCLE_R() {
        return SMALL_CIRCLE_R;
    }

    static get BIG_CIRCLE_R() {
        return BIG_CIRCLE_R;
    }

    static get SCREEN_WIDTH_LIMIT() {
        return SCREEN_WIDTH_LIMIT;
    }

    static get WIDTH_COEFFICIENT_BIG_SCREEN() {
        return WIDTH_COEFFICIENT_BIG_SCREEN;
    }

    static get WIDTH_COEFFICIENT_SMALL_SCREEN() {
        return WIDTH_COEFFICIENT_SMALL_SCREEN;
    }

    static get GALLERY_CUTOUT_DEFAULT_HEIGHT() {
        return GALLERY_CUTOUT_DEFAULT_HEIGHT;
    }

    static get GALLERY_CUTOUT_DEFAULT_WIDTH() {
        return GALLERY_CUTOUT_DEFAULT_WIDTH;
    }
}